rm -rf dest
mkdir -p dest
git ls-files | while read i; do
  if ! [[ $i = dest/* ]] && ! [[ $i = rename ]]; then
    cp "$i" "dest/${i//\//!}"
  fi
done
cd dest
sed -i s/project_name/%%%PROJECT_NAME%%%/g *
sed -i s/Project_name/%%%MODULE_NAME%%%/g *
for i in *project_name* ; do mv $i ${i/project_name/PROJECT_NAME}; done
cd ..
